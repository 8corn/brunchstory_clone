<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>

<p>
  <strong>User:</strong>
  <%= @post.account.email %>
</p>


<%= link_to 'Edit', edit_category_post_path(@post.category, @post) %> |
<%= link_to 'Back', category_path(@post.category) %>
<%= link_to 'Delete', category_posts_path(@post.category, @post), method: :delete, data: { confirm: '정말 삭제하시겠습니까?' } %>