<!DOCTYPE html>
<html>
  <head>
	  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel ="stylesheet" crossorigin="anonymous">
	  
	  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
	  
	  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
	  
    <title>GoormRailsApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

	
	
  <body>
	  <div>
		  <% if account_signed_in? %>
			  <%= link_to "회원정보", edit_account_registration_path %> |
			  <%= link_to "로그아웃", destroy_account_session_path, mathod: :delete %> |
			  아이디 : <%= current_account.email %> |
			  유저 NO. : <%= current_account.id %>
		  <% else %>
			  <%= link_to "로그인", account_session_path %>
			  <%= link_to "회원가입", "/accounts/signup" %>
		  <% end %>
	  </div>
	  <hr />
    <%= yield %>
  </body>
</html>
